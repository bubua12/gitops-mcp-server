apiVersion: v1
kind: Service
metadata:
  name: gitops-mcp-server
  namespace: gitops-mcp
  labels:
    app.kubernetes.io/name: gitops-mcp-server
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 18080
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: gitops-mcp-server
